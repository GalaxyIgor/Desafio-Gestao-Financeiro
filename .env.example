# ==========================================================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE (versionado — sem valores reais)
# Copie este arquivo para .env.local e preencha os valores:
#   cp .env.example .env.local
# ==========================================================================

# URL base da API de finanças (server-only, SEM prefixo NEXT_PUBLIC_).
API_BASE_URL=

# Segredo para proteger o cookie de sessão (valor aleatório forte).
SESSION_SECRET=
